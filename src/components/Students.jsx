import React from 'react';
import PropTypes from 'prop-types';

const Students = props => {
  return (
    <div>
      Students
    </div>
  );
};

Students.propTypes = {

};

export default Students;
