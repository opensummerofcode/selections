import React, { useState, useEffect } from 'react';

import styles from '../assets/styles/dashboard.module.css';

const Filters = () => {
  return <header className={styles.filters}>Filters</header>;
};

export default Filters;
