import React, { useState, useEffect } from 'react';

import styles from '../assets/styles/dashboard.module.css';

const Filters = () => {
  return <header className={styles.filters} />;
};

export default Filters;
